# Project Instructions

- **Deployment Platform:** Always use Vercel for deployment.
- **Framework:** React (Vite) + Express (Full-stack).
- **Vercel Configuration:** Maintain `vercel.json` and ensure `server.ts` is compatible with serverless functions (exporting the app instance).
